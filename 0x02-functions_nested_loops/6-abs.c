#include "main.h"

/**
 * _abs - The function name
 * @n: The value to find the absolute value
 * Return: The absolute value of the number
 */
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	return (n);
}
